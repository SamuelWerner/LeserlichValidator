var express = require('express');
var router = express.Router();


/* GET home page. */
router.get('/', function(req, res) {
    req.body.url
    res.render('waiting', { title: 'Leserator.de'});
});

module.exports = router;

